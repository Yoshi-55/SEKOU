<!DOCTYPE html>
<html>
  <head>
    <title>施工募集 - カーラッピング・フリート職人マッチング</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50 min-h-screen flex flex-col">
    <!-- Header -->
    <header class="bg-white border-b border-gray-200 sticky top-0 z-50">
      <nav class="container mx-auto px-6 py-4">
        <div class="flex justify-between items-center">
          <div class="flex items-center space-x-12">
            <%= link_to root_path, class: "text-2xl font-bold bg-gradient-to-r from-blue-600 to-blue-800 bg-clip-text text-transparent" do %>
              施工募集
            <% end %>

            <% if user_signed_in? %>
              <div class="hidden md:flex space-x-6">
                <%= link_to "案件を探す", jobs_path, class: "text-gray-600 hover:text-blue-600 font-medium transition-colors" %>
                <%= link_to "案件作成", new_job_path, class: "text-gray-600 hover:text-blue-600 font-medium transition-colors" %>
                <%= link_to "グループ", groups_path, class: "text-gray-600 hover:text-blue-600 font-medium transition-colors" %>
                <%= link_to "応募履歴", applies_path, class: "text-gray-600 hover:text-blue-600 font-medium transition-colors" %>
                <%= link_to "募集履歴", posted_jobs_path, class: "text-gray-600 hover:text-blue-600 font-medium transition-colors" %>
              </div>
            <% end %>
          </div>

          <div class="flex items-center space-x-4">
            <% if user_signed_in? %>
              <%= link_to profile_path, class: "text-gray-700 hover:text-blue-600 font-medium transition-colors" do %>
                <%= current_user.name %>さん
              <% end %>
              <%= button_to "ログアウト", destroy_user_session_path, method: :delete, class: "bg-gray-100 hover:bg-gray-200 text-gray-700 px-5 py-2.5 rounded-lg font-medium transition-all" %>
            <% else %>
              <%= link_to "ログイン", new_user_session_path, class: "text-gray-700 hover:text-blue-600 font-medium transition-colors" %>
              <%= link_to "新規登録", new_user_registration_path, class: "bg-gradient-to-r from-blue-600 to-blue-700 hover:from-blue-700 hover:to-blue-800 text-white px-6 py-2.5 rounded-lg font-medium shadow-sm hover:shadow transition-all" %>
            <% end %>
          </div>
        </div>
      </nav>
    </header>

    <!-- Flash Messages -->
    <% if notice.present? %>
      <div class="container mx-auto px-6 mt-6">
        <div class="bg-green-50 border border-green-200 text-green-800 px-5 py-4 rounded-xl flex items-center shadow-sm" role="alert">
          <svg class="w-5 h-5 mr-3" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
          </svg>
          <%= notice %>
        </div>
      </div>
    <% end %>

    <% if alert.present? %>
      <div class="container mx-auto px-6 mt-6">
        <div class="bg-red-50 border border-red-200 text-red-800 px-5 py-4 rounded-xl flex items-center shadow-sm" role="alert">
          <svg class="w-5 h-5 mr-3" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd"/>
          </svg>
          <%= alert %>
        </div>
      </div>
    <% end %>

    <!-- Main Content -->
    <main class="container mx-auto px-6 py-12 flex-grow">
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="bg-gray-900 text-white mt-auto">
      <div class="container mx-auto px-6 py-12">
        <div class="grid grid-cols-1 md:grid-cols-3 gap-12">
          <div>
            <h3 class="text-xl font-bold mb-4 bg-gradient-to-r from-blue-400 to-blue-600 bg-clip-text text-transparent">施工募集について</h3>
            <p class="text-gray-400 leading-relaxed">カーラッピング・フリート施工現場と職人をマッチングするプラットフォームです。</p>
          </div>
          <div>
            <h3 class="text-xl font-bold mb-4">リンク</h3>
            <ul class="text-gray-400 space-y-3">
              <li><%= link_to "利用規約", "#", class: "hover:text-white transition-colors" %></li>
              <li><%= link_to "プライバシーポリシー", "#", class: "hover:text-white transition-colors" %></li>
              <li><%= link_to "運営会社", "#", class: "hover:text-white transition-colors" %></li>
            </ul>
          </div>
          <div>
            <h3 class="text-xl font-bold mb-4">お問い合わせ</h3>
            <p class="text-gray-400">info@example.com</p>
          </div>
        </div>
        <div class="border-t border-gray-800 mt-12 pt-8 text-center text-gray-500">
          <p>&copy; 2026 施工募集. All rights reserved.</p>
        </div>
      </div>
    </footer>
  </body>
</html>
