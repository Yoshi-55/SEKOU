<!DOCTYPE html>
<html>
  <head>
    <title>施工募集 - カーラッピング・フリート職人マッチング</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <style>
      @media (max-width: 767px) {
        body { font-size: 17px; }
        input, select, textarea, button { font-size: 16px !important; }
      }
    </style>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50 min-h-screen flex flex-col">
    <!-- Header -->
    <header class="bg-white border-b border-gray-200 sticky top-0 z-50">
      <nav class="container mx-auto px-4 py-4">
        <div class="flex justify-between items-center">
          <div class="flex items-center space-x-8">
            <%= link_to root_path, class: "text-2xl font-bold bg-gradient-to-r from-blue-600 to-blue-800 bg-clip-text text-transparent" do %>
              施工募集
            <% end %>

            <% if user_signed_in? %>
              <div class="hidden md:flex space-x-6">
                <%= link_to "案件を探す", jobs_path, class: "text-gray-600 hover:text-blue-600 font-medium transition-colors" %>
                <%= link_to "案件作成", new_job_path, class: "text-gray-600 hover:text-blue-600 font-medium transition-colors" %>
                <%= link_to "グループ", groups_path, class: "text-gray-600 hover:text-blue-600 font-medium transition-colors" %>
                <%= link_to "応募履歴", applies_path, class: "text-gray-600 hover:text-blue-600 font-medium transition-colors" %>
                <%= link_to "募集履歴", posted_jobs_path, class: "text-gray-600 hover:text-blue-600 font-medium transition-colors" %>
              </div>
            <% end %>
          </div>

          <div class="flex items-center space-x-3">
            <% if user_signed_in? %>
              <div class="hidden md:flex items-center space-x-3">
                <%= link_to profile_path, class: "text-gray-700 hover:text-blue-600 font-medium transition-colors" do %>
                  <%= current_user.name %>さん
                <% end %>
                <%= button_to "ログアウト", destroy_user_session_path, method: :delete, class: "bg-gray-100 hover:bg-gray-200 text-gray-700 px-4 py-2 rounded-lg font-medium transition-all text-sm" %>
              </div>
            <% else %>
              <div class="hidden md:flex items-center space-x-3">
                <%= link_to "ログイン", new_user_session_path, class: "text-gray-700 hover:text-blue-600 font-medium transition-colors" %>
                <%= link_to "新規登録", new_user_registration_path, class: "bg-gradient-to-r from-blue-600 to-blue-700 hover:from-blue-700 hover:to-blue-800 text-white px-5 py-2 rounded-lg font-medium shadow-sm hover:shadow transition-all text-sm" %>
              </div>
            <% end %>

            <button id="mobile-menu-button" onclick="toggleMobileMenu()" class="md:hidden p-3 rounded-xl text-gray-600 hover:text-blue-600 hover:bg-gray-100 transition-colors" aria-label="メニュー">
              <svg id="hamburger-icon" class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M4 6h16M4 12h16M4 18h16"/>
              </svg>
              <svg id="close-icon" class="w-8 h-8 hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M6 18L18 6M6 6l12 12"/>
              </svg>
            </button>
          </div>
        </div>

        <div id="mobile-menu" class="hidden md:hidden border-t border-gray-200 mt-3 pt-2 pb-3">
          <% if user_signed_in? %>
            <div class="space-y-1">
              <%= link_to "案件を探す", jobs_path, class: "block px-4 py-4 rounded-xl text-gray-800 hover:text-blue-600 hover:bg-blue-50 font-bold text-lg transition-colors" %>
              <%= link_to "案件を作成する", new_job_path, class: "block px-4 py-4 rounded-xl text-gray-800 hover:text-blue-600 hover:bg-blue-50 font-bold text-lg transition-colors" %>
              <%= link_to "グループ", groups_path, class: "block px-4 py-4 rounded-xl text-gray-800 hover:text-blue-600 hover:bg-blue-50 font-bold text-lg transition-colors" %>
              <%= link_to "応募履歴", applies_path, class: "block px-4 py-4 rounded-xl text-gray-800 hover:text-blue-600 hover:bg-blue-50 font-bold text-lg transition-colors" %>
              <%= link_to "募集履歴", posted_jobs_path, class: "block px-4 py-4 rounded-xl text-gray-800 hover:text-blue-600 hover:bg-blue-50 font-bold text-lg transition-colors" %>
              <div class="border-t border-gray-200 mt-2 pt-2 space-y-1">
                <%= link_to profile_path, class: "block px-4 py-4 rounded-xl text-gray-800 hover:text-blue-600 hover:bg-blue-50 font-bold text-lg transition-colors" do %>
                  <%= current_user.name %>さんのプロフィール
                <% end %>
                <%= button_to "ログアウト", destroy_user_session_path, method: :delete, class: "w-full text-left px-4 py-4 rounded-xl text-red-600 hover:bg-red-50 font-bold text-lg transition-colors" %>
              </div>
            </div>
          <% else %>
            <div class="space-y-2 pt-1">
              <%= link_to "ログイン", new_user_session_path, class: "block px-4 py-4 rounded-xl text-gray-800 hover:text-blue-600 hover:bg-blue-50 font-bold text-lg transition-colors" %>
              <%= link_to "新規登録", new_user_registration_path, class: "block px-4 py-4 rounded-xl bg-blue-600 text-white hover:bg-blue-700 font-bold text-lg transition-colors text-center mt-2" %>
            </div>
          <% end %>
        </div>
      </nav>
    </header>

    <script>
      function toggleMobileMenu() {
        var menu = document.getElementById('mobile-menu');
        var hamburger = document.getElementById('hamburger-icon');
        var close = document.getElementById('close-icon');
        menu.classList.toggle('hidden');
        hamburger.classList.toggle('hidden');
        close.classList.toggle('hidden');
      }
    </script>

    <!-- Flash Messages -->
    <% if notice.present? %>
      <div class="container mx-auto px-4 md:px-6 mt-4 md:mt-6">
        <div class="bg-green-50 border border-green-200 text-green-800 px-4 py-3 md:px-5 md:py-4 rounded-xl flex items-center shadow-sm" role="alert">
          <svg class="w-5 h-5 mr-3" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
          </svg>
          <%= notice %>
        </div>
      </div>
    <% end %>

    <% if alert.present? %>
      <div class="container mx-auto px-4 md:px-6 mt-4 md:mt-6">
        <div class="bg-red-50 border border-red-200 text-red-800 px-4 py-3 md:px-5 md:py-4 rounded-xl flex items-center shadow-sm" role="alert">
          <svg class="w-5 h-5 mr-3" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd"/>
          </svg>
          <%= alert %>
        </div>
      </div>
    <% end %>

    <!-- Main Content -->
    <main class="container mx-auto px-4 md:px-6 py-6 md:py-12 flex-grow">
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="bg-gray-900 text-white mt-auto">
      <div class="container mx-auto px-6 py-12">
        <div class="grid grid-cols-1 md:grid-cols-3 gap-12">
          <div>
            <h3 class="text-xl font-bold mb-4 bg-gradient-to-r from-blue-400 to-blue-600 bg-clip-text text-transparent">施工募集について</h3>
            <p class="text-gray-400 leading-relaxed">カーラッピング・フリート施工現場と職人をマッチングするプラットフォームです。</p>
          </div>
          <div>
            <h3 class="text-xl font-bold mb-4">リンク</h3>
            <ul class="text-gray-400 space-y-3">
              <li><%= link_to "利用規約", terms_path, class: "hover:text-white transition-colors" %></li>
              <li><%= link_to "プライバシーポリシー", privacy_path, class: "hover:text-white transition-colors" %></li>
              <li><%= link_to "運営会社", company_path, class: "hover:text-white transition-colors" %></li>
            </ul>
          </div>
          <div>
            <h3 class="text-xl font-bold mb-4">お問い合わせ</h3>
            <p class="text-gray-400">info@example.com</p>
          </div>
        </div>
        <div class="border-t border-gray-800 mt-12 pt-8 text-center text-gray-500">
          <p>&copy; 2026 施工募集. All rights reserved.</p>
        </div>
      </div>
    </footer>
  </body>
</html>
