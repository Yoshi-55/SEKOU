<!DOCTYPE html>
<html>
  <head>
    <title>施工募集 - カーラッピング・フリート職人マッチング</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50">
    <!-- Header -->
    <header class="bg-white shadow-sm">
      <nav class="container mx-auto px-4 py-4">
        <div class="flex justify-between items-center">
          <div class="flex items-center space-x-8">
            <%= link_to root_path, class: "text-2xl font-bold text-blue-600" do %>
              施工募集
            <% end %>

            <% if user_signed_in? %>
              <div class="flex space-x-4">
                <%= link_to "案件を探す", jobs_path, class: "text-gray-700 hover:text-blue-600" %>
                <%= link_to "案件作成", new_job_path, class: "text-gray-700 hover:text-blue-600" %>
                <%= link_to "応募履歴", applies_path, class: "text-gray-700 hover:text-blue-600" %>
              </div>
            <% end %>
          </div>

          <div class="flex items-center space-x-4">
            <% if user_signed_in? %>
              <%= link_to profile_path, class: "text-gray-700 hover:text-blue-600" do %>
                <%= current_user.name %>さん
              <% end %>
              <%= button_to "ログアウト", destroy_user_session_path, method: :delete, class: "bg-gray-200 hover:bg-gray-300 text-gray-800 px-4 py-2 rounded" %>
            <% else %>
              <%= link_to "ログイン", new_user_session_path, class: "text-gray-700 hover:text-blue-600" %>
              <%= link_to "新規登録", new_user_registration_path, class: "bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded" %>
            <% end %>
          </div>
        </div>
      </nav>
    </header>

    <!-- Flash Messages -->
    <% if notice.present? %>
      <div class="container mx-auto px-4 mt-4">
        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded" role="alert">
          <%= notice %>
        </div>
      </div>
    <% end %>

    <% if alert.present? %>
      <div class="container mx-auto px-4 mt-4">
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded" role="alert">
          <%= alert %>
        </div>
      </div>
    <% end %>

    <!-- Main Content -->
    <main class="container mx-auto px-4 py-8">
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="bg-gray-800 text-white mt-16">
      <div class="container mx-auto px-4 py-8">
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
          <div>
            <h3 class="text-xl font-bold mb-4">施工募集について</h3>
            <p class="text-gray-300">カーラッピング・フリート施工現場と職人をマッチングするプラットフォームです。</p>
          </div>
          <div>
            <h3 class="text-xl font-bold mb-4">リンク</h3>
            <ul class="text-gray-300 space-y-2">
              <li><%= link_to "利用規約", "#", class: "hover:text-white" %></li>
              <li><%= link_to "プライバシーポリシー", "#", class: "hover:text-white" %></li>
              <li><%= link_to "運営会社", "#", class: "hover:text-white" %></li>
            </ul>
          </div>
          <div>
            <h3 class="text-xl font-bold mb-4">お問い合わせ</h3>
            <p class="text-gray-300">info@example.com</p>
          </div>
        </div>
        <div class="border-t border-gray-700 mt-8 pt-8 text-center text-gray-400">
          <p>&copy; 2026 施工募集. All rights reserved.</p>
        </div>
      </div>
    </footer>
  </body>
</html>
