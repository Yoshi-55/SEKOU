<!-- Hero Section -->
<div class="bg-gradient-to-r from-blue-600 to-blue-800 text-white rounded-lg shadow-lg p-12 mb-12">
  <div class="max-w-3xl">
    <h1 class="text-4xl md:text-5xl font-bold mb-4">
      職人と職人を<br>
      マッチングするプラットフォーム
    </h1>
    <p class="text-xl mb-8">
      カーラッピング・フリートなどの施工案件を簡単に掲載・応募できます
    </p>
    <div class="flex gap-4">
      <% if user_signed_in? %>
        <%= link_to "案件を掲載する", new_job_path, class: "bg-white text-blue-600 px-8 py-3 rounded-lg font-bold hover:bg-gray-100 transition" %>
      <% else %>
        <%= link_to "案件を掲載する", new_user_registration_path, class: "bg-white text-blue-600 px-8 py-3 rounded-lg font-bold hover:bg-gray-100 transition" %>
      <% end %>
      <%= link_to "案件を探す", jobs_path, class: "border-2 border-white text-white px-8 py-3 rounded-lg font-bold hover:bg-white hover:text-blue-600 transition" %>
    </div>
  </div>
</div>

<!-- Features Section -->
<div class="grid md:grid-cols-3 gap-8 mb-12">
  <div class="bg-white rounded-lg shadow p-6">
    <div class="text-blue-600 text-4xl mb-4">🎯</div>
    <h3 class="text-xl font-bold mb-2">簡単な案件掲載</h3>
    <p class="text-gray-600">
      必要事項を入力するだけで、すぐに案件を掲載できます。基本無料で利用可能です。
    </p>
  </div>

  <div class="bg-white rounded-lg shadow p-6">
    <div class="text-blue-600 text-4xl mb-4">💰</div>
    <h3 class="text-xl font-bold mb-2">基本無料(DEMO)</h3>
    <p class="text-gray-600">
      案件の掲載は基本無料。登録料や月額費用も一切かかりません。
    </p>
  </div>

  <div class="bg-white rounded-lg shadow p-6">
    <div class="text-blue-600 text-4xl mb-4">⚡</div>
    <h3 class="text-xl font-bold mb-2">スピーディーな対応</h3>
    <p class="text-gray-600">
      案件を作成すればすぐに公開されます。職人からの応募もリアルタイムで確認できます。
    </p>
  </div>
</div>

<!-- Recent Jobs Section -->
<div class="bg-white rounded-lg shadow p-8">
  <h2 class="text-2xl font-bold mb-6">新着案件</h2>
  <% if @recent_jobs.present? %>
    <div class="space-y-4">
      <% @recent_jobs.each do |job| %>
        <%= link_to job_path(job), class: "block border rounded-lg p-4 hover:shadow-md transition" do %>
          <div>
            <h3 class="font-bold text-lg mb-1"><%= job.title %></h3>
            <p class="text-gray-600 text-sm mb-2"><%= truncate(job.description, length: 100) %></p>
            <div class="flex gap-4 text-sm text-gray-500">
              <span>📍 <%= job.location %></span>
              <span>💰 <%= number_to_currency(job.budget, unit: "¥", precision: 0) %></span>
              <span>📅 <%= l(job.scheduled_date, format: :short) %></span>
            </div>
          </div>
        <% end %>
      <% end %>
    </div>
    <div class="text-center mt-6">
      <%= link_to "すべての案件を見る", jobs_path, class: "text-blue-600 hover:underline" %>
    </div>
  <% else %>
    <p class="text-gray-500 text-center py-8">現在、公開中の案件はありません</p>
  <% end %>
</div>

