<div class="max-w-3xl mx-auto">
  <div class="text-center mb-8">
    <div class="inline-flex items-center justify-center w-20 h-20 bg-green-100 rounded-full mb-4">
      <svg class="w-12 h-12 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
      </svg>
    </div>
    <h1 class="text-3xl font-bold mb-2">決済が完了しました</h1>
    <p class="text-gray-600">案件が公開されました</p>
  </div>

  <!-- Payment Summary -->
  <div class="bg-white rounded-lg shadow p-6 mb-6">
    <h2 class="text-xl font-bold mb-4">決済情報</h2>
    <div class="space-y-3">
      <div class="flex justify-between">
        <span class="text-gray-600">決済日時</span>
        <span class="font-medium"><%= l(@payment.paid_at, format: :long) if @payment.paid_at %></span>
      </div>
      <div class="flex justify-between">
        <span class="text-gray-600">決済金額</span>
        <span class="font-bold text-lg text-blue-600"><%= number_to_currency(@payment.amount, unit: "¥", precision: 0) %></span>
      </div>
      <div class="flex justify-between">
        <span class="text-gray-600">決済ID</span>
        <span class="font-mono text-sm"><%= @payment.stripe_charge_id %></span>
      </div>
    </div>
  </div>

  <!-- Job Details -->
  <div class="bg-white rounded-lg shadow p-6 mb-6">
    <h2 class="text-xl font-bold mb-4">公開された案件</h2>
    <div class="space-y-3">
      <div>
        <p class="text-sm text-gray-600 mb-1">案件タイトル</p>
        <p class="font-bold text-lg"><%= @job.title %></p>
      </div>
      <div class="grid grid-cols-2 gap-4">
        <div>
          <p class="text-sm text-gray-600 mb-1">場所</p>
          <p class="font-medium">📍 <%= @job.location %></p>
        </div>
        <div>
          <p class="text-sm text-gray-600 mb-1">予算</p>
          <p class="font-medium"><%= number_to_currency(@job.budget, unit: "¥", precision: 0) %></p>
        </div>
        <div>
          <p class="text-sm text-gray-600 mb-1">作業予定日</p>
          <p class="font-medium"><%= l(@job.scheduled_date, format: :long) %></p>
        </div>
        <div>
          <p class="text-sm text-gray-600 mb-1">掲載期限</p>
          <p class="font-medium"><%= l(@job.expires_at, format: :long) if @job.expires_at %></p>
        </div>
      </div>

      <!-- Options -->
      <% if @job.featured? || @job.urgent? || @job.extended_period? %>
        <div class="border-t pt-3 mt-3">
          <p class="text-sm text-gray-600 mb-2">適用オプション</p>
          <div class="flex gap-2 flex-wrap">
            <% if @job.featured? %>
              <span class="bg-yellow-100 text-yellow-800 text-xs px-3 py-1 rounded-full font-medium">注目表示</span>
            <% end %>
            <% if @job.urgent? %>
              <span class="bg-red-100 text-red-800 text-xs px-3 py-1 rounded-full font-medium">急募</span>
            <% end %>
            <% if @job.extended_period? %>
              <span class="bg-green-100 text-green-800 text-xs px-3 py-1 rounded-full font-medium">掲載期間60日</span>
            <% end %>
          </div>
        </div>
      <% end %>
    </div>
  </div>

  <!-- Notice -->
  <div class="bg-blue-50 border border-blue-200 rounded p-4 mb-6">
    <p class="text-sm text-gray-700">
      ※案件は職人が検索できる状態になりました<br>
      ※応募があった場合、登録されたメールアドレスに通知が届きます<br>
      ※案件の詳細や応募者の管理は案件詳細ページから行えます
    </p>
  </div>

  <!-- Actions -->
  <div class="flex gap-4">
    <%= link_to "案件詳細を見る", job_path(@job), class: "flex-1 bg-blue-600 hover:bg-blue-700 text-white px-8 py-3 rounded-lg font-bold text-center" %>
    <%= link_to "案件一覧に戻る", jobs_path, class: "px-8 py-3 rounded-lg font-medium text-gray-700 hover:bg-gray-100 border border-gray-300" %>
  </div>
</div>
